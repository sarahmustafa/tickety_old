<%= form_for(@theater) do |f| %>
  <% if @theater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theater.errors.count, "error") %> prohibited this theater from being saved:</h2>

      <ul>
      <% @theater.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cinema_id %><br />
    <%= f.number_field :cinema_id %>
  </div>
  <div class="field">
    <%= f.label :theater_number %><br />
    <%= f.number_field :theater_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
